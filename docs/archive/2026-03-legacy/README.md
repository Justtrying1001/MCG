# Legacy archive (2026-03)

Ce dossier contient des documents de phase, audits ponctuels et plans d’implémentation historiques.

## Important
- Ces documents **ne sont pas la source de vérité runtime**.
- La source de vérité actuelle est dans `docs/technical/*` et le code (`app/api`, `lib/domain`, `prisma/schema.prisma`).

## Comment les lire
- Utiliser ces docs uniquement pour contexte historique (décisions passées, migrations, vocabulaire legacy).
- Ne pas dériver de comportement produit actuel depuis ces fichiers sans vérification dans le code.
