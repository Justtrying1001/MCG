# Roadmap 01 — Internal Demo

Required: reliable controlled walkthrough of core collection + tournament utility loop, without wallet friction blocking flow.

Rarity cleanup framing:
- Rarity may still exist in runtime during demo.
- Demo messaging should remain edition-first.
- If rarity appears in UI, treat it as known legacy/runtime mismatch.
